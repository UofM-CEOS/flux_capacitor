2013-10-23  spluque@gmail.com

	* day_splitter.pro:
	  No need to supply a third argument to match2, and any
	  manipulations with that output argument.

	* nav_std_template.sav:
	  Updating field names.

2013-10-16  spluque@gmail.com

	* std2.pro:
	  Check for valid UTC times and subset *before* parsing times.

	* std.pro:
	  Check for valid UTC times and subset *before* parsing times.

	* std2.pro:
	  Check for valid UTC times. Skip if bad.

	* process_RAD.pro:
	  Use /continue in message if no matching file found.

2013-10-15  spluque@gmail.com

	* nav_raw_template.sav:
	  Use UTC instead of GPS time name.

	* truewind.pro:
	  This reproduces results in paper, with arrays as input.

2013-10-11  spluque@gmail.com

	* std.pro:
	  Lower case names for comparisons.

	* met_std_template.sav, rad_std_template.sav:
	  Updated template field names.

	* average_series.pro:
	  Fixed erroneous extraction of minutes in output hash.

	* rad_raw_template.sav, rad_std_template.sav:
	  Adding new template and fixing RAD raw template.

	* parse_times.pro:
	  Assume that no seconds means setting them to zero.

	* std.pro:
	  Correct manipulations for RAD files.

	* omg_hdg_std_template.sav, omg_nav_std_template.sav:
	  Assume that no seconds means setting them to zero.

	* locate_time_strings.pro:
	  Relax check for missing seconds. We assume that no seconds means
	  setting it to zero in parse_times().

	* average_series.pro:
	  Proper checks for what arguments we got.

2013-10-09  spluque@gmail.com

	* std.pro, std2.pro:
	  Fixed message call at the last case statement.

	* omg_hdg_raw_template.sav, omg_nav_raw_template.sav:
	  Modified template's time names so that it contains a leading
	  string to indicate what sort of time stamp it is (assuming UTC).
	  This is needed so that we can parse the time strings.

	* average_series.pro:
	  Eliminating unused argument.

	* met_raw_template.sav:
	  Modified template's time names so that it contains a leading
	  string to indicate what sort of time stamp it is (assuming UTC).
	  This is needed so that we can parse the time strings.

	* std_met.pro:
	  Generalized procedure for any file with a single time input
	  column, or where other time input columns just don't matter.

	* std2.pro:
	  Exit if no input files.

	* average_series.pro:
	  Generalized averaging for angles and magnitudes.

	* day_splitter.pro:
	  Reverting to output entire day's data, as it is required for
	  averaging.

	* std2.pro:
	  Set invalid time numbers to the empty string prior to passing to
	  parse_times().

	* day_splitter.pro:
	  Fix silly mistake when ignoring other non-time groups.

	* nav_std_template.sav:
	  Changing template specification to match output from STD2.

	* parse_times.pro:
	  Fixed typo (important).

2013-10-08  spluque@gmail.com

	* filter_series.pro, std_gyro.pro, std_met.pro, std_nav.pro,
	  std_omg.pro:
	  Correct input and output file search.

	* day_splitter.pro:
	  Correct input file search.

2013-10-07  spluque@gmail.com

	* std_gyro.pro:
	  Fixed output second format.

	* filter_series.pro:
	  Working revision.

	* doy2calendar.pro:
	  Format seconds as float to allow for sub-second frequencies.

	* day_splitter.pro:
	  Use time parsing code, changing argument name to STEP_TIME.

2013-10-04  spluque@gmail.com

	* std_gyro.pro:
	  Label output time as UTC, as assumed.

	* gyro_std_template.sav:
	  Corrected field names.

2013-10-03  spluque@gmail.com

	* std_gyro.pro, std_met.pro, std_omg.pro, std_rmc.pro:
	  Successfully factored out parsing of times.

	* parse_times.pro:
	  Utility function for parsing times.

	* day_splitter.pro:
	  Do not write file if we have no data for a day.

	* std_met.pro:
	  Use new function to determine how to extract time info.

	* std_rmc.pro:
	  Use 'server' instead of 'gps' to refer to the time info that
	  corresponds to the computer or whatever means data were
	  downloaded from.

	* met_raw_template.sav:
	  Use DOY instead of julday.

2013-10-02  spluque@gmail.com

	* std_omg.pro:
	  Output only one decimal place for seconds.

	* gyro_std_template.sav, omg_hdg_raw_template.sav,
	  omg_hdg_std_template.sav, omg_nav_std_template.sav:
	  Adding templates.

	* std_omg.pro:
	  Working version. Preparing to rename.

	* std_gyro.pro:
	  Check if we have any tags to remove at the end.

	* rmc_raw_template.sav:
	  Corrected template.

2013-10-01  spluque@gmail.com

	* day_splitter.pro, nav_avg.pro, std_met.pro, std_nav.pro,
	  std_omg.pro, std_rmc.pro:
	  Use MESSAGE instead of PRINT.

	* std_rmc.pro:
	  Take seconds from whatever seconds field we have through the end
	  of it.

	* std_rmc.pro:
	  Working version.

2013-09-28  spluque@gmail.com

	* nav_avg.pro:
	  Now works with a hash.

	* flux_startup.pro:
	  Update to reflect new name.

	* stddev_yamartino.pro, stdev_yamartino.pro:
	  Renaming for consistency.

	* day_splitter.pro:
	  Fix subscripting.

	* flux_startup.pro:
	  Added modules.

	* flux_capacitor.pro:
	  Update procedure calls.

	* control.inc:
	  Update variables.

2013-09-27  spluque@gmail.com

	* day_splitter_NAV.pro:
	  No longer needed.

	* match2.pro, nav_std_template.sav, std_nav.pro:
	  Adding file and updating template.

	* flux_startup.pro:
	  Compile utilities first.

	* day_splitter.pro:
	  Use match2 and eliminate looping through records.

	* day_splitter.pro, nav_std_template.sav, nav_template.sav:
	  New NAV standard template, and adjust call to day_splitter.

	* day_splitter.pro, day_splitter_MET.pro:
	  Renaming to just day_splitter.pro, since this should be the main
	  splitting program.

	* remove_structure_tag.pro:
	  Extract scalar if only one name is supplied.

2013-09-26  spluque@gmail.com

	* day_splitter_MET.pro:
	  Properly working version.

	* met_raw_template.sav, met_std_template.sav:
	  Time fields should be strings.

	* std_met.pro:
	  Do not work on arguments directly! IDL passes arguments by
	  reference.

	* doy2calendar.pro:
	  Do not work on the arguments directly! IDL passes arguments by
	  reference.

2013-09-25  spluque@gmail.com

	* std_met.pro:
	  Better argument handling, and correct number of lines to iterate
	  over.

2013-09-24  spluque@gmail.com

	* day_splitter_MET.pro, met_raw_template.sav, met_template.sav:
	  Renaming file.

	* nav_avg.pro:
	  Do not use temporary() for argument manipulations!

	* met_template.sav:
	  Rename field in itemplate.

	* met_template.sav:
	  Rename template to itemplate.

	* control.inc:
	  Improved header variables.

2013-09-23  spluque@gmail.com

	* control.inc:
	  Standardize header strings.

2013-09-20  spluque@gmail.com

	* nav_avg.pro:
	  Use stddev(); stdev() is obsolete.

	* day_splitter_MET.pro:
	  Working version, but not doing all the checks original does.

	* control.inc:
	  Improved header strings.

	* doy2calendar.pro:
	  Do not supply Null to caldat!

	* std_nav.pro:
	  Correct usage.

	* std_nav.pro:
	  Do not change directory. Rename variables.

	* day_splitter_NAV.pro:
	  Correct number of arguments in initial check.

	* day_splitter_NAV.pro:
	  Remove n_prefix argument; not used.

	* day_splitter_MET.pro:
	  Startup procedures.

	* day_splitter_MET.pro, day_splitter_NAV.pro, day_splitter_nav.pro:
	  Adding file, and renaming NAV file.

	* nav_avg.pro:
	  Working version.

2013-09-19  spluque@gmail.com

	* bearing_avg.pro:
	  First take vectorized function.

2013-09-18  spluque@gmail.com

	* bearing_avg.pro:
	  Efforts to vectorize this function.

	* nav_avg.pro:
	  Corrected usage info.

	* nav_avg.pro:
	  Fixed creation of all_arr to account for output sample rate, and
	  finished procedure for filling fields of non-angular averages.

	* nav_avg.pro, one_min_nav.pro:
	  Renaming file.

	* control.inc, day_splitter_nav.pro, doy2calendar.pro,
	  flux_capacitor.pro, nav_template.sav, one_min_nav.pro,
	  setDifference.pro, std_nav.pro:
	  Adding source from most recent work from iMac at work.

2013-09-16  Sebastian Luque  <spluque@gmail.com>

	* std_nav.pro:
	Modified arguments to account for convention of directories without
	trailing path separators.

	* day_splitter_nav.pro: Finished first take, working properly.

2013-09-13  Sebastian Luque  <spluque@gmail.com>

	* day_splitter_nav.pro: Implemented proper removal of unneeded days.

2013-09-12  Sebastian Luque  <spluque@gmail.com>

	* doy2calendar.pro: Reformat output of JUL2TIMESTAMP.

	* doy2calendar.pro: Added JUL2TIMESTAMP function.

2013-09-11  Sebastian Luque  <spluque@gmail.com>

	* day_splitter_nav.pro: Trying structure approach (TEST).

2013-09-04  Sebastian Luque  <spluque@gmail.com>

	* std_nav.pro:
	Format the output without so much whitespace, to reduce file size and
	facilitate operations later.

	* doy2calendar.pro: Return a string also for calendar2doy.

	* std_nav.pro: Use different output file name.

2013-09-03  Sebastian Luque  <spluque@gmail.com>

	* std_nav.pro: Remove old comment.

	* std_nav.pro: Rename procedure and file, and add checks.

2013-09-03  Sebastian Luque  <spluque@gmail.com>

	* std_nav.pro: Rename procedure and file, and add checks.

2013-08-28  Sebastian Luque  <spluque@gmail.com>

	* doy2calendar.pro: Added 'calendar2doy' function.

	* flux_capacitor.pro:
	Do not create temporary 'val' variable that is just for tests; just use the
	expression in the test.

	* std_nav_AN11.pro:
	Added a test to skip rows with improper number of columns.  In reality,
	this needs a much more thorough test for the structure of input.  It is
	very fragile for now.

	Removed the 'out_columns' argument, since it doesn't serve any
	purpose because the input columns are hard-coded, which implies
	fixed output columns.
